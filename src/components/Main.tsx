import React from 'react';
import Layers from '@components/layers/Layers';

function Main() {
	return <div>
		<React.StrictMode>
			<Layers />
		</React.StrictMode>
	</div>;
}

export default Main;
